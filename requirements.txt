# =============================================================================
# สภาพแวดล้อมสำหรับโปรเจกต์วิเคราะห์ท่าวิ่ง (MediaPipe + Random Forest)
# =============================================================================
# ผลการทดลองทั้งหมดในรายงาน (Accuracy 78.5% / macro-F1 0.774 / LOSO 0.714±0.125)
# รันบนสภาพแวดล้อมนี้:
#
#   Python 3.12.10  บน  Windows 11 (AMD64)
#
# ติดตั้งด้วย:  pip install -r requirements.txt
#
# -----------------------------------------------------------------------------
# ⚠️ ข้อควรระวังเรื่อง OpenCV — อ่านก่อนติดตั้ง
# -----------------------------------------------------------------------------
# เครื่องต้นทางที่ใช้พัฒนามี OpenCV ติดตั้งซ้อนกัน 3 distribution พร้อมกัน
# (opencv-python 5.0.0.93 / opencv-contrib-python 4.11.0.86 /
#  opencv-python-headless 4.13.0.92) ซึ่งทั้งสามตัวเขียนไฟล์ลงโฟลเดอร์ `cv2/`
# เดียวกัน ตัวที่ import ได้จริงคือ opencv-python 5.0.0.93 (cv2.__version__ = 5.0.0)
#
# บนเครื่องใหม่ให้ติดตั้ง **opencv-python ตัวเดียวเท่านั้น** ตามที่ระบุด้านล่าง
# ห้ามติดตั้ง opencv-contrib-python หรือ opencv-python-headless เพิ่ม เพราะจะ
# เขียนทับกันแล้วทำให้เวอร์ชันที่ถูกใช้จริงเปลี่ยนไปโดยไม่มีการแจ้งเตือน
#
# โค้ดในโปรเจกต์ใช้เฉพาะฟังก์ชันหลักของ OpenCV (VideoCapture, VideoWriter,
# cvtColor, resize) ไม่มีการใช้โมดูล contrib และไม่มีการใช้ GUI (imshow/waitKey)
#
# -----------------------------------------------------------------------------
# ⚠️ ไฟล์ที่ต้องดาวน์โหลดแยก (ไม่ใช่แพ็กเกจ pip)
# -----------------------------------------------------------------------------
# pose_landmarker.task — โมเดล MediaPipe Pose Landmarker **รุ่น heavy** (~30.6 MB)
# วางไว้ที่รากของโปรเจกต์ (ระดับเดียวกับไฟล์นี้)
# ดาวน์โหลด: https://developers.google.com/mediapipe/solutions/vision/pose_landmarker
# ห้ามใช้รุ่น lite (~5 MB) หรือ full (~9 MB) แทน เพราะผลการทดลองทั้งหมดใช้รุ่น heavy
# =============================================================================


# --- แกนหลัก: pipeline ขั้นที่ 00-05 (สกัดข้อมูล / เทรน / ประเมินผล) ---------
mediapipe==1.0.1              # ตรวจจับ pose landmark (01_extract_landmarks.py)
opencv-python==5.0.0.93       # อ่าน/เขียนวิดีโอ + letterbox (00_resize_videos.py)
scikit-learn==1.9.0           # RandomForest, GroupKFold, metrics (03-06)
numpy==2.5.2                  # คำนวณเวกเตอร์และมุมข้อต่อ (02_prepare_dataset.py)
pandas==2.2.3                 # จัดการตารางข้อมูล landmark (ทุกขั้นตอน)
pyarrow==24.0.0               # engine สำหรับอ่าน/เขียน .parquet (pandas ไม่ได้ติดมาให้)
joblib==1.4.2                 # บันทึก/โหลด model.joblib

# --- เว็บแอปพลิเคชัน: 07_web/ -------------------------------------------------
fastapi==0.141.1              # REST API (07_web/app.py)
uvicorn==0.49.0               # ASGI server สำหรับรัน FastAPI
python-multipart==0.0.32      # จำเป็นสำหรับรับไฟล์อัปโหลด (FastAPI ต้องการ)

# --- เสริม: เฉพาะ 06_compare_models.py (เปรียบเทียบ RF / GB / MLP) ------------
# ถ้าไม่ต้องรันการเปรียบเทียบโมเดล สามารถข้ามบรรทัดนี้ได้
imbalanced-learn==0.14.2      # SMOTE + imblearn.Pipeline สำหรับ MLP
